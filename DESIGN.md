# DESIGN.md — визуальное оформление и UX

Инструмент оформлен в том же дизайн-языке, что и основной сайт
[design-dmitriev.github.io](https://design-dmitriev.github.io/): швейцарский
минимализм — белый лист, чёрный шрифт Onest, волосяные линии, нумерованные
разделы, одно главное действие на экран, без цветных акцентов.

JS (Three.js-сцена, экспорт SVG/PDF, загрузка макета) находит элементы
только по `id` — при правках разметки **все id сохранять**, классы и
вложенность можно менять свободно. Сцена сама подстраивается под размер
`#stage` (ResizeObserver).

## Раскладка

- **Компьютер (≥ 900px):** 3D-сцена слева на всю высоту экрана, настройки —
  колонка 400px справа со своей прокруткой. Блок «Развёртка» закреплён внизу
  колонки — скачать можно в любой момент, не листая.
- **Телефон/планшет (< 900px):** сцена сверху (58% высоты экрана), под ней
  настройки обычным списком, «Развёртка» — последним разделом.
- **Управление вскрытием** (кнопка «Открыть» + ползунок этапов) — плавающая
  панель поверх сцены, рядом с тем, чем управляет. На телефоне показываются
  только первый и последний этап, чтобы подписи не слипались.
- **Полноэкранный 3D** — скрывает шапку и настройки, остаются сцена,
  управление вскрытием и «Закрыть».

## Разделы настроек (01–05)

1. **Тип коробки** — вертикальный список с радио-индикатором вместо
   сегмент-контрола: длинные названия не обрезаются, выбор виден сразу.
2. **Размер** — поля сеткой 3 в ряд (на телефоне 2), крупные (48px), без
   стрелок-спиннеров. Сброс — явной ссылкой «↺ Сбросить: 85 × 150 × 20»
   (раньше была непонятная кнопка с одними цифрами).
3. **Вид** — чекбоксы строками на всю ширину, нажимается вся строка.
4. **Дизайн** — зона загрузки с подсказкой форматов и про перетаскивание;
   после загрузки рамка становится сплошной.
5. **Развёртка** — «Скачать PDF» главной чёрной кнопкой, SVG — второстепенной.

## Удобство

- Все кнопки/поля/строки — от 44px по высоте (минимум для пальца).
- Шрифт в полях ввода 16px — iPhone не приближает страницу при фокусе.
- `env(safe-area-inset-*)` — шапка, кнопка полноэкранного режима и панель
  вскрытия не заезжают под вырез экрана и полоску жестов.
- Видимый фокус с клавиатуры (`:focus-visible`), `prefers-reduced-motion`
  отключает переходы.

## История

- 2026-09-25 — перекрашено в Ч/Б (убран розовый `#ff3d8b`), тёмная тема.
- 2026-09-25 — UX-переработка: светлая швейцарская тема, боковая колонка на
  компьютере, нумерованные разделы, панель вскрытия поверх сцены, крупные
  поля и понятный сброс размеров, исправлено перекрытие настроек на телефоне.
