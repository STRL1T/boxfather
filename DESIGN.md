# DESIGN.md — визуальное оформление

Инструмент переоформлен под тот же дизайн-язык, что и основной сайт
[design-dmitriev.github.io](https://design-dmitriev.github.io/) (проект
Portfolio_v3): строгий Ч/Б Swiss-минимализм, без цветных акцентов, шрифт
Onest, пилюли-кнопки, тонкие полупрозрачные разделители.

## Что изменено (2026-09-25)

Правки только в `<style>`-блоке `index.html` — HTML-структура, id/классы,
на которые завязан JS (Three.js-сцена, экспорт SVG/PDF, обработчики), **не
трогались**.

- Убран розовый акцент (`--pink: #ff3d8b`) — везде заменён на чистый белый/чёрный:
  активная кнопка/сегмент = белая заливка + чёрный текст, как `ArrowButton`
  и активный `SectionLabel` на основном сайте.
- Фон `#141315` → `#0a0a0a` (тот же тёмный, что в Hero/Оферта/Контакты
  основного сайта), панели/поля — оттенки того же чёрного, а не отдельная
  серо-фиолетовая гамма.
- Линии/рамки — `rgba(255,255,255,.08–.14)` вместо непрозрachного
  `#2f2c33` — тоньше и светлее, в духе `border-line` основного сайта.
- Кнопки: скругление 999px (пилюля), hover — лёгкое затемнение (`opacity`),
  а не смена цвета — единообразно с `ArrowButton.tsx`.

## Мобильная адаптация

- Touch-таргеты (кнопки, сегменты, поля ввода, загрузка макета) — не
  меньше 44×44px на экранах уже 560px (см. `RULES.md` основного сайта,
  правило 2).
- `env(safe-area-inset-*)` учтён в шапке (сверху) и в полноэкранном
  3D-режиме на мобильных (снизу/слева/справа) — контролы не заезжают под
  чёлку/индикатор жестов.
- Полноэкранный 3D-режим (`body.full`) и адаптивная раскладка панели
  (`@media max-width: 560px`) — логика не менялась, только цвета/отступы.

## Что осознанно не трогали

- 3D-сцену (Three.js: материалы, свет, фон canvas) — она не хардкодит
  старый акцентный цвет, отдельная зона ответственности от UI-хрома.
- Шрифт Onest уже был подключён (Google Fonts) — совпадает с основным
  сайтом, менять не пришлось.
